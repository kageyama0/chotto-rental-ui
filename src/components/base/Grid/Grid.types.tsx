import type {
  GridProps as MantineGridProps,
  GridColProps as MantineGridColProps,
} from '@mantine/core';

export interface GridProps extends MantineGridProps {}
export interface GridColProps extends MantineGridColProps {}
